/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Tambahkan domain gambar dari template ThemeForest di sini jika perlu
    remotePatterns: [],
  },
};

module.exports = nextConfig;
